const GITHUB_USER = "DouarMC";
const GITHUB_REPO = "minecraft-bedrock-creators-utilities";

/**
 * L'URL de base pour accéder aux fichiers de l'extension, comme les fichiers de schémas json ou les versions des modules du script api.
 */
export const SCHEMA_BASE_URL = `https://${GITHUB_USER}.github.io/${GITHUB_REPO}/`;